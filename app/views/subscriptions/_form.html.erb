<%= form_with(model: subscription) do |form| %>
  <% if subscription.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
        <% subscription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :subscribed_id, style: "display: block" %>
    <%= form.text_field :subscribed_id %>
  </div>

  <div>
    <%= form.label :subscriber_id, style: "display: block" %>
    <%= form.text_field :subscriber_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
